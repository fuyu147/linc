#include "matrix/matrix.h"
#include "vector/vector.h"
#include <stddef.h>

int main()
{
        if (0)
                matrixExample();
        if (1)
                vectorExample();

        return 0;
}
